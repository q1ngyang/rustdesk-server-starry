# patch-v1.3.1 is under development. Publication remains blocked until the
# dual-role FastMedia bind/forward/fallback/re-entry and release gates pass for
# the exact immutable candidate commit.
status: BLOCKED
patch_version: 1.3.1

# Do not change this to APPROVED from implementation work alone.
