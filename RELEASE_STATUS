# FastMedia renewal v1 was frozen before implementation in commits
# 1844654a272d70112bbbc7774414320c98aa3b99 and
# f65981fbf8c77dd93cfd026422e528128aa13d1c. The reviewed implementation
# candidate e44f6a0380914454cc543ebb6cdb031f5b3e08f9 passed source,
# controlled-clock, contract, overlay, lint, test, and build gates. Stable
# publication remains blocked on immutable real-Akari long-session,
# cross-network NAT/UDP fault-soak, and hosted-artifact provenance.
# PREVIEW_APPROVED permits only a GitHub prerelease and rolling `preview`
# image; it never replaces `latest`.
status: BLOCKED
patch_version: 1.3.2

# Do not change this to APPROVED from preview or diagnostic validation alone.
# Relay Reallocation v1 is an unfrozen development candidate. Preview is also
# blocked until its dual-end probe/scoring, two-HBBR rollback, FastMedia
# generation fencing and controlled-clock long-session gates are complete.
